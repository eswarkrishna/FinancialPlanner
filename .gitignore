node_modules
dist
*.local
.DS_Store
.env
.env.*
!.env.example
!.env.development
!.env.production
coverage
*.log
.vite

# Cursor local usage capture (sessionEnd hook); may contain prompts — do not commit
.cursor/usage/

# Android/Capacitor generated artifacts (keep source under android/ trackable)
android/.gradle/
android/build/
android/**/build/
android/local.properties

# Encrypted brainstorm — keep plaintext out of git
docs/research/2026-07-client-growth-productivity-brainstorm.md
