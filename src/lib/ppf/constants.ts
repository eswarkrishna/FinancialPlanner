/** Government-notified PPF rate placeholder — verify against official sources (§4.18). */
export const DEFAULT_PPF_ANNUAL_RATE_PCT = 7.1;

/** Statutory annual PPF contribution cap (INR) for UI hint only. */
export const PPF_ANNUAL_CONTRIBUTION_CAP_INR = 150_000;
